/** Auto-refresh interval for active trip data (ms). */
export const ACTIVE_TRIP_REFRESH_MS = 60 * 1000;

/** Countdown timer update interval (ms). */
export const COUNTDOWN_UPDATE_MS = 60 * 1000;

/** Google Maps URL template for "View on Maps" (q=query). */
export const MAPS_QUERY_URL = 'https://www.google.com/maps/search/?api=1&query=';
